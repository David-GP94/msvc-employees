spring:
  application:
    name: msvc-employees
  server:
    port: 8080
  datasource:
    url: jdbc:mysql://localhost:3306/technical_test?useSSL=false&serverTimezone=UTC
    username: root
    password: sasa
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true